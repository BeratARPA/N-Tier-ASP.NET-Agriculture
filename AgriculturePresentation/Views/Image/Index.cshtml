@using EntityLayer.Concrete
@model List<Image>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <div class="card-body">
        <div class="form-group">
            <a class="btn btn-outline-info" href="/Image/AddImage/">Ekle</a>
        </div>

        <div class="table-responsive">
            <table class="table table-hover table-bordered">
                <tr>
                    <th>#</th>
                    <th>#</th>
                    <th>Başlık</th>
                    <th>Açıklama</th>
                    <th>#</th>
                    <th>#</th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.ImageId</td>
                        <td><img src="@item.ImageUrl" alt="image" /></td>
                        <td>@item.Title</td>
                        <td>@item.Description</td>
                        <td><a class="btn btn-outline-success" href="/Image/UpdateImage/@item.ImageId">Güncelle</a></td>
                        <td><a class="btn btn-outline-danger" href="/Image/DeleteImage/@item.ImageId">Sil</a></td>
                    </tr>
                }
            </table>
        </div>
    </div>
</div>